name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build and Test
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        rust: [stable]

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Install Rust toolchain
      uses: dtolnay/rust-toolchain@stable
      with:
        components: rustfmt, clippy

    - name: Cache cargo registry
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

    - name: Build
      run: cargo build --profile ci --all-features

    - name: Run tests
      run: |
        cargo test --profile ci --all-features --lib
        cargo test --profile ci --all-features --test integration_test --test security_test --test performance_test --test fuzzing_test --test penetration_test --test proptest_invariants
      timeout-minutes: 20

    - name: Check formatting
      run: cargo fmt -- --check
      continue-on-error: true

    - name: Clippy
      run: cargo clippy --all-features -- -D warnings
      continue-on-error: true

  elara-integration:
    name: ELARA Integration Tests
    runs-on: ubuntu-latest
    # ELARA tests are #[ignore]d (hang in CI); job runs quickly, keeps build manifest
    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive
    - uses: dtolnay/rust-toolchain@stable
    - uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
    - run: cargo test --profile ci --all-features --test elara_integration_test

  enterprise-relay:
    name: Enterprise API & Relay
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: dtolnay/rust-toolchain@stable
    - run: cargo build --manifest-path enterprise-api/Cargo.toml
    - run: cargo build --manifest-path b4ae-relay/Cargo.toml

  proptest:
    name: Proptest Invariants
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Run proptest invariants
      run: cargo test --test proptest_invariants --all-features
      env:
        # 32 cases to balance coverage vs CI time; edge cases covered by test_message_roundtrip_edge_cases
        PROPTEST_CASES: 32

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Install cargo-audit
      run: cargo install cargo-audit
      continue-on-error: true

    - name: Cargo audit
      run: cargo audit
      continue-on-error: true

    - name: Security audit script
      run: chmod +x scripts/security_audit.sh && FULL_AUDIT=0 ./scripts/security_audit.sh
      continue-on-error: true

  cargo-fuzz:
    name: Cargo Fuzz (libFuzzer)
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Install Rust nightly
      uses: dtolnay/rust-toolchain@nightly
      with:
        components: rustfmt

    - name: Install cargo-fuzz
      run: cargo install cargo-fuzz

    - name: Build fuzz targets
      run: |
        cd fuzz
        cargo fuzz build
      continue-on-error: true

    - name: Run fuzz (short run)
      run: |
        cd fuzz
        cargo fuzz run fuzz_handshake -- -runs=100 -max_total_time=10
        cargo fuzz run fuzz_message -- -runs=100 -max_total_time=10
      continue-on-error: true

  tla:
    name: TLA+ TLC Model Check
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: 17

    - name: Download TLC
      run: |
        mkdir -p tla
        curl -sL -o tla/tla2tools.jar "https://github.com/tlaplus/tlaplus/releases/download/v1.7.4/tla2tools.jar"

    - name: Run TLC
      run: |
        cd specs
        java -XX:+UseParallelGC -jar ../tla/tla2tools.jar -deadlock -config B4AE_Handshake.cfg B4AE_Handshake.tla

  wasm:
    name: WASM Build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: wasm32-unknown-unknown

    - name: Install wasm-pack
      run: cargo install wasm-pack

    - name: Build WASM
      run: wasm-pack build b4ae-wasm --target web --out-dir /tmp/pkg

  ffi:
    name: C FFI (b4ae-ffi)
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Build b4ae-ffi
      run: cargo build --release --manifest-path b4ae-ffi/Cargo.toml

  android:
    name: Android JNI (b4ae-android)
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Build b4ae-android
      run: cargo build --release --manifest-path b4ae-android/Cargo.toml

  coq:
    name: Coq Formal Verification
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Build Coq spec
      run: |
        docker run --rm -v $PWD/specs/coq:/work:ro -w /tmp coqorg/coq:latest sh -c "cp /work/B4AE_Handshake.v /tmp/ && coqc B4AE_Handshake.v"
